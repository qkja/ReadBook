// 分组交换 -- 就是把众多的报文集合出来,然后按照一定的顺序进行转发
// TCP/IP 协议具有开放性和实用性两大特点
#include <iostream>
#include <math.h>
using namespace std;

int main()
{
  printf("总字节数 %lu\n", (unsigned long)pow(2.0, 32));
  printf("%lu\n", (unsigned long)pow(2.0, 32));
  //cout << pow(2.0, 32) << endl;
  return 0;
}
